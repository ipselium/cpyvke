TODO :

	- "s" to acces directly to menu save
	- Kernel manager (restart dead kernel ?)
	- Daemon : Fix bugs !
	- Resize in external function (to apply resize even in submenus)
	- Option des kernels :

		from jupyter_client import manager
		kmanager, kclient = manager.start_new_kernel(extra_arguments=["--matplotlib='inline'"])
